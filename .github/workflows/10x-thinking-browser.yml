name: 10x thinking browser playtest

on:
  pull_request:
    paths:
      - 'apps/10x-thinking/**'
      - '.github/workflows/10x-thinking-browser.yml'
  push:
    branches:
      - main
    paths:
      - 'apps/10x-thinking/**'
      - '.github/workflows/10x-thinking-browser.yml'
  workflow_dispatch:

jobs:
  mobile-playtest:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install browser harness
        run: |
          npm install --no-save playwright@1.55.0
          npx playwright install --with-deps chromium
      - name: Check JavaScript syntax
        run: node --check apps/10x-thinking/app.js
      - name: Start static server
        run: |
          python3 -m http.server 4173 --bind 127.0.0.1 > /tmp/10x-server.log 2>&1 &
          for i in $(seq 1 30); do
            if curl -fsS 'http://127.0.0.1:4173/apps/10x-thinking/?test=1' >/dev/null; then exit 0; fi
            sleep 1
          done
          cat /tmp/10x-server.log
          exit 1
      - name: Run real mobile playtest
        env:
          TENX_URL: http://127.0.0.1:4173/apps/10x-thinking/?test=1
        run: node apps/10x-thinking/smoke-browser.mjs
      - name: Run LEVEL UP quality gate
        run: node scripts/validate-levelup-quality-gate.mjs --app apps/10x-thinking
      - name: Upload playtest evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: 10x-thinking-browser-playtest
          path: apps/10x-thinking/.artifacts/*
          if-no-files-found: warn
          retention-days: 7
          include-hidden-files: true
      - name: Server log on failure
        if: failure()
        run: cat /tmp/10x-server.log || true
