# すける。

背面カメラ映像を全画面表示し、その上を不透明なスクラッチ面で覆うスマホ向け実験アプリ。

画面を指でこするとスクラッチ面が削れ、こすり続けると最終的に全面が透明になってカメラ映像だけが残る。

- `getUserMedia` / `facingMode: environment` で背面カメラを優先
- カメラ映像は保存・送信しない
- Pointer Eventsで指の軌跡を取得
- Canvasの `destination-out` でこすった部分を消す
- 一定量こすると全面透明化
- 「曇らせる」でリセット
- HTTPS必須
