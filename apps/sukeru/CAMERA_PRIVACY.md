# カメラ利用

このアプリはブラウザの `navigator.mediaDevices.getUserMedia()` を利用して端末のカメラ映像を画面内に表示します。

- 音声は取得しません。
- 背面カメラを優先します。
- 映像の録画、画像保存、アップロード、外部送信は実装していません。
- ページを閉じるとカメラトラックを停止します。
- カメラ利用にはHTTPSとユーザーの許可が必要です。
