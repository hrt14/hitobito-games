='f'?'active':''}" data-g="f"><b>女性</b><small>見た目だけに影響</small></button></div></div><div class="field"><label>性格</label><div class="choice-row four">${pers.map(x=>`<button class="choice ${creator.personality===x[0]?'active':''}" data-p="${x[0]}"><b>${x[1]}</b><small>${x[2]}</small></button>`).join('')}</div></div></section><div class="creator-footer"><button class="primary" id="go">この町で暮らす</button></div></main>`;document.getElementById('name').oninput=e=>creator.name=e.target.value.trim()||'ユウ';document.querySelectorAll('[data-g]').forEach(b=>b.onclick=()=>{creator.gender=b.dataset.g;creator.face=creator.gender==='m'?'🙂':'😊';creatorScreen()});document.querySelectorAll('[data-p]').forEach(b=>b.onclick=()=>{creator.personality=b.dataset.p;creatorScreen()});document.getElementById('go').onclick=start}
function nav(active){return`<nav class="bottomnav"><button class="nav-btn ${active==='world'?'active':''}" data-nav="world"><strong>🏘</strong>暮らす</button><button class="nav-btn ${active==='map'?'active':''}" data-nav="map"><strong>🗺</strong>町</button><button class="nav-btn ${active==='history'?'active':''}" data-nav="history"><strong>📖</strong>歴史</button><button class="nav-btn ${active==='family'?'active':''}" data-nav="family"><strong>🌳</strong>家族</button></nav>`}function bind(){document.querySelectorAll('[data-nav]').forEach(b=>b.onclick=()=>{view=b.dataset.nav;render()})}
function token(p){return`<button class="person-token" data-person="${p.id}"><span class="person-bubble">${p.action}</span><span class="mini-face ${p.age<15?'child':p.age>=65?'elder':''}" style="--shirt:${p.shirt}">${face(p)}</span><span class="mini-body" style="--shirt:${p.shirt}"></span><span class="person-name">${p.first}</span></button>`}
function world(){let m=me(),loc=P(m.location),ps=alive().filter(p=>!p.isPlayer&&p.location===m.location).slice(0,7),sug=ps[0];app.innerHTML=`<main class="phone"><section class="world"><header class="topbar"><div class="date">${S.year}年 ${season()}・${S.day}日<small>${phase()} ${String(S.hour).padStart(2,'0')}:00　${S.weather}</small></div><div class="top-actions"><button class="icon-btn" id="speed">×${S.speed}</button><button class="icon-btn" id="map">🗺</button></div></header><div class="location-head"><div class="location-title">${loc.icon} ${loc.name}</div><div class="location-desc">${loc.desc}</div></div><div class="scene ${loc.scene}"><div class="mountain"></div><div class="mountain m2"></div><div class="landmark"></div><div class="hill-tree"></div><div 